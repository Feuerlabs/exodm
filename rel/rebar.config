%% -*- erlang -*-
{plugins, [exodm_rebar_plugin]}.
{pa, "../ebin"}.

%% Add the apps that need to be included in the generated release
{rel_apps, "exodm",
	    [kernel,stdlib,sasl,
	     {setup,load}, {exodm,load},
	     gproc, exo, yang, bert, reactor,
	     eleveldb,kvdb,mnesia,unsplit]}.
